@model List<Aluno>

<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Alunos</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6">
            </div>
            <div class="col-6 text-end">
                <a asp-controller="Aluno" asp-action="Create" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i>  Criar novo aluno
                </a>
            </div>
        </div>

        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>
                        Nome
                    </th> <th>
                        Idade
                    </th>
                    <th>
                        Numero
                    </th> 
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model.OrderBy(u => u.Numero))
                {
                    <tr>
                        <td>@obj.Nome</td>
                        <td>@obj.Idade</td>
                        <td>@obj.Numero</td>
                       
                        <td>
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="Aluno" asp-action="#" asp-route-id="@obj.Id" class="btn btn-primary mx-2">
                                    <i class="bi bi-pencil-square"></i> Edit
                                </a>
                                <a asp-controller="Aluno" asp-action="#" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                                    <i class="bi bi-trash-fill"></i> Delete
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

    </div>
</div>